@using RealStateApp.Core.Application.ViewModels.PropertyType;
@model List<PropertyTypeViewModel>;

<head>
    <link rel="stylesheet" href="~/css/table.css" />
    <script src="https://kit.fontawesome.com/7302c44b99.js" crossorigin="anonymous"></script>
</head>

@if (Model.Any())
{
<div class="container-fluid">
    <div class="col-8 offset-2">
        <a asp-controller="PropertyType" asp-action="Save" class="btn btn-primary mt-4">Register</a>
        <div class="card mt-2">
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>Descripcion</th>
                                <th>Propiedades utilizando este tipo:</th>
                                <th>Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var propertyType in Model)
                            {
                                <tr>
                                    <td>@propertyType.PropertyTypeName</td>
                                    <td>@propertyType.Description</td>
                                    <td>@propertyType.PropertiesAmount</td>
                                    <td>
                                        <a asp-route-Id="@propertyType.Id" asp-controller="PropertyType" asp-action="Remove" class="btn btn-outline-danger"><i class="fa-solid fa-trash-can"></i></a>
                                        <a asp-route-Id="@propertyType.Id" asp-controller="PropertyType" asp-action="Edit" class="btn btn-outline-warning"><i class="fa-solid fa-pencil"></i></a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
}
else
{
    <div class="col-6 offset-3 mt-4">
        <div class="card text-center">
            <div class="card-body">
                <i class="fa-solid fa-house-circle-check fa-5x text-muted mb-3"></i>
                <h5 class="card-title">No hay tipo de propiedades registradas</h5>
                <a asp-controller="PropertyType" asp-action="Save" class="btn btn-primary">Register</a>
            </div>
        </div>
    </div>
}
